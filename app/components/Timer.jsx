var React = require('react');

// Stateless functional component
var Timer = (props) => {
  return (
    <div>
      <div>
        <div>
          <h3>Timer.jsx Rendered</h3>
        </div>
      </div>
    </div>
  );
};

module.exports = Timer;
